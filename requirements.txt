# Runtime dependencies for camera_service.py.
#
# The media pipeline runs in a system ffmpeg subprocess, so there is no
# OpenCV / NumPy / Pillow dependency - Python never touches pixels.
#
# System packages also required:
#   ffmpeg, fonts-dejavu-core, mosquitto
flask>=3.0
pyserial>=3.5
paho-mqtt>=2.0
cheroot>=10.0
